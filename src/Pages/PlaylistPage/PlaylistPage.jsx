import { PlaylistCard, PlaylistHeader } from "./PlaylistPageComponents";

export const PlaylistPage = () => {
  return (
    <section>
      <PlaylistHeader />
      <PlaylistCard />
    </section>
  );
};
